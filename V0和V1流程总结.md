# V0 和 V1 流程总结

## V0 流程（基础版本）

### 输入
- `initial_kol_500.json` - 500 个 KOL 账号列表
- `tweets_14d.jsonl` - 14 天推文数据
- `tweets_24h.jsonl` - 24 小时推文数据

### 处理流程

**Step C: ETL 清洗与标准化**
- C.1 账号全集校验：只保留 500 个 KOL 的推文
- C.2 时间字段标准化：创建 created_ts, created_at_local, local_date
- C.3 文本字段标准化：生成 raw_text, clean_text（URL 替换为 <URL>）
- C.4 entities 标准化：hashtags/mentions 转小写，urls 解析 domain，生成 url_domains
- C.5 结构化去重：按 tweet_id 去重

**Step D: 传播关系派生**
- 从 referenced_tweets 生成传播边（reposted/quoted）
- 输出：edges_14d.jsonl, edges_24h.jsonl

**Step E: 候选主题生成**
- E.1 Hashtag Topic：从 entities.hashtags 生成（资本科技过滤）
- E.2 Cashtag Topic：从 entities.cashtags 生成（不过滤）
- E.3 Domain Topic：从 url_domains 生成（资本科技过滤）
- E.4 Center Tweet：从 edges_24h 中被引用的推文生成（资本科技过滤）
- 输出：topic_candidates_24h.jsonl

**Step F: Topic 聚合**
- 按 (topic_type, topic_key) 分组
- 统计：tweet_cnt, unique_author_cnt, sum_like_cnt, sum_repost_cnt, sum_reply_cnt, sum_quote_cnt, ref_cnt
- 特殊处理：center_tweet 的 unique_author_cnt 统计转发/引用的 KOL 数量
- 输出：topics_24h.jsonl

**Step G: 热度评分与 Top5**
- 计算 HotScore（区分 center_tweet 和其他类型）
- 过滤：优先选择多 KOL 讨论的话题（unique_author_cnt >= 2 或 tweet_cnt >= 2）
- 生成可读信息：title, description, example_tweets
- 输出：hot_topics_top5.json

**Step H: 趋势判断**
- 构建 daily time series（14 天数据）
- 趋势标签：Emerging, Rising, Peak, Fading, Persistent
- 输出：trends_14d.jsonl

**Step I: Daily Report 输出**
- 整合 Top5 热点和趋势信息
- 生成完整报告和易读版本（中文）
- 输出：daily_report.json, daily_report_readable.json

### V0 输出文件
- tweets_14d_clean.jsonl
- tweets_24h_clean.jsonl
- edges_14d.jsonl
- edges_24h.jsonl
- topic_candidates_24h.jsonl
- topics_24h.jsonl
- hot_topics_top5.json
- trends_14d.jsonl
- daily_report.json
- daily_report_readable.json

---

## V1 流程（高级版本）

### 输入
- `initial_kol_500.json` - 500 个 KOL 账号列表
- `tweets_14d.jsonl` - 14 天推文数据
- `tweets_24h.jsonl` - 24 小时推文数据

### 处理流程

**Step C: ETL 清洗与标准化**（与 V0 相同）
- C.1 账号全集校验
- C.2 时间字段标准化
- C.3 文本字段标准化
- C.4 entities 标准化
- C.5 结构化去重

**Step D: 传播关系派生**（与 V0 相同）
- 生成传播边

**Step E: 候选主题生成**（增强版）
- E.1 Hashtag Topic
- E.2 Cashtag Topic
- E.3 Domain Topic
- E.4 Center Tweet
- **E.5 Content Keyword（V1 新增）**：从推文内容提取关键词（TF-IDF，词频统计）

**Step F: Topic 聚合**（与 V0 相同）
- 按 (topic_type, topic_key) 分组统计

**Step F': 语义 Topic Clustering（V1 新增）**
- 为每个 topic 构造语义文本表示
- 生成 Embedding 向量（256维改进 TF-IDF 或 OpenAI）
- 聚类（DBSCAN eps=0.4 或简单距离聚类，相似度阈值 0.75）
- 构建 semantic_topics 和 topic_to_semantic_id 映射
- 输出：semantic_topics_24h_advanced.jsonl

**Step G': 基于 Semantic Topic 的热度评分与 Top5（V1 新增）**
- 聚合所有 member topics 的指标
- 计算 HotScore（包含关键词奖励 keyword_bonus）
- Top5 选择：优先选择多 KOL + 包含关键词的真实话题
- 生成可读信息：从 member_topics 提取关键词作为标题
- 输出：hot_topics_top5_v1_advanced.json

**Step H: 趋势判断**（与 V0 相同）
- 构建 daily time series
- 趋势标签判定

**Step H': 热点发展预测（V1 新增）**
- 特征工程（增强版）：
  - author_growth_rate_1d（作者增长率）
  - momentum_3d（3天动量）
  - momentum_7d（7天动量）
  - volatility（波动性）
  - avg_3d_author, avg_7d_author（3天/7天平均作者数）
- 预测逻辑：使用多个特征综合判断（grow/stabilize/fade）
- 输出：growth_predictions_24h_advanced.jsonl

**Step I: Daily Report 输出**（增强版）
- 整合 Top5 热点、趋势、预测信息
- 生成完整报告和易读版本（包含预测）
- 输出：daily_report_v1_advanced.json, daily_report_readable_v1_advanced.json

### V1 输出文件

**基础输出**（与 V0 相同）：
- tweets_14d_clean.jsonl
- tweets_24h_clean.jsonl
- edges_14d.jsonl
- edges_24h.jsonl
- topic_candidates_24h.jsonl（包含 content_keyword）
- topics_24h.jsonl（包含 content_keyword）
- trends_14d.jsonl（包含 content_keyword）

**V1 新增输出**：
- semantic_topics_24h_advanced.jsonl
- hot_topics_top5_v1_advanced.json
- growth_predictions_24h_advanced.jsonl
- daily_report_v1_advanced.json
- daily_report_readable_v1_advanced.json

---

## V0 vs V1 核心区别

| 方面 | V0 | V1 |
|------|----|----|
| **候选主题** | 4 种（hashtag, cashtag, domain, center_tweet） | 5 种（+ content_keyword） |
| **Topic 粒度** | 符号级（单个 hashtag/domain/tweet） | 语义级（多个相关 topic 聚合成 cluster） |
| **聚类** | 无 | 语义聚类（256维 embedding，DBSCAN/距离聚类） |
| **HotScore** | 基础公式（区分 center_tweet） | 增强公式（关键词奖励，语义 topic 聚合） |
| **Top5 选择** | 多 KOL 过滤 | 多 KOL + 关键词过滤（质量优先） |
| **标题生成** | 基于 topic_key | 从 member_topics 提取关键词 |
| **趋势分析** | 后验标签（Emerging/Rising/Peak/Fading/Persistent） | 后验标签 + 预测（grow/stabilize/fade） |
| **预测特征** | 无 | 增强特征（增长率、动量、波动性） |
| **报告内容** | 静态报告 | 静态 + 预测性报告 |

---

## 流程对比图

### V0 流程
```
输入 → C(ETL) → D(传播边) → E(候选主题) → F(聚合) → G(评分Top5) → H(趋势) → I(报告) → 输出
```

### V1 流程
```
输入 → C(ETL) → D(传播边) → E(候选主题+关键词) → F(聚合) 
      → F'(语义聚类) → G'(语义评分Top5) → H(趋势) → H'(预测) → I(增强报告) → 输出
```

---

## 关键优化点

### V1 相比 V0 的优化
1. **语义聚类**：解决同一热点被拆成多个 topic 的问题
2. **内容关键词**：从推文内容提取更多关键词，不局限于 hashtag/domain/cashtag
3. **质量优先**：Top5 选择更严格，确保都是多 KOL 讨论的真实话题
4. **预测能力**：从"发现热点"升级到"预测走势"
5. **增强特征**：使用增长率、动量、波动性等更丰富的特征

